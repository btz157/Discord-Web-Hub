services:
  - type: web
    name: ws-store-bot
    runtime: node
    region: oregon
    plan: free
    buildCommand: >
      npm install -g pnpm@latest &&
      pnpm install --no-frozen-lockfile &&
      BASE_PATH=/ pnpm --filter @workspace/bot-dashboard run build &&
      pnpm --filter @workspace/api-server run build &&
      mkdir -p artifacts/api-server/public &&
      cp -r artifacts/bot-dashboard/dist/public/. artifacts/api-server/public/
    startCommand: node --enable-source-maps artifacts/api-server/dist/index.mjs
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "10000"
      - key: DATABASE_URL
        sync: false
      - key: DISCORD_TOKEN
        sync: false
      - key: DISCORD_CLIENT_ID
        sync: false
      - key: DISCORD_CLIENT_SECRET
        sync: false
      - key: DISCORD_GUILD_ID
        sync: false
      - key: SESSION_SECRET
        generateValue: true

databases:
  - name: ws-store-db
    databaseName: wsstorebot
    plan: free
